name: caminho_do_saber
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.0.0 
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_messaging: ^15.0.0
  flutter_local_notifications: ^17.1.2
  google_sign_in: ^6.2.1
  provider: ^6.1.2
  lottie: ^3.1.2
  flutter_html: ^3.0.0-beta.2
  html: ^0.15.4
  shared_preferences: ^2.3.2
  audioplayers: ^5.2.1
  card_flip: ^0.0.5
  firebase_storage: ^12.0.0
  http: ^1.2.2
  path_provider: ^2.1.4
  uuid: ^4.4.0
  image_picker: ^1.1.2
  firebase_app_check: ^0.3.0
  fl_chart: ^0.68.0
  flutter_tts: ^4.2.2
  video_player: ^2.9.2 # NOVO
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.5.24
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.11
  drift_dev: ^2.18.0

  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
    - assets/data/
    - assets/sounds/
    - assets/avatars/
    - assets/video/ # NOVO
